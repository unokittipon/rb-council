# คู่มือใช้งาน Find Your Future

หน้า "Find Your Future" รวมค่าย ทุน กิจกรรม และโอกาสเรียนต่อ
ข้อมูลทั้งหมด**ดึงจาก Google Sheet โดยตรง** — สภาแก้ Sheet → เว็บอัปเดตเองใน 1-5 นาที
ไม่ต้อง push git ไม่ต้องเขียนโค้ด

---

## 🚀 Quick Start (ตั้งครั้งแรก)

### 1. สร้าง Google Sheet

ไปที่ [sheets.google.com](https://sheets.google.com) → สร้างไฟล์ใหม่ ตั้งชื่อ เช่น "RB Find Your Future"

### 2. ใส่หัวคอลัมน์ในแถวที่ 1

Copy บรรทัดนี้แล้ววางในแถวที่ 1 ของ Sheet:

```
title	category	detail	requirement	fee	link	apply_date	apply_date_iso	event_date	event_date_iso
```

> ⚠️ **ตัวเล็กทั้งหมด สะกดตรงเป๊ะ** — ระบบอ่านตามชื่อหัว ลำดับสลับได้

### 3. Publish เป็น CSV

1. `File` → `Share` → `Publish to web`
2. ช่องซ้าย: เลือก Sheet ที่มีข้อมูล
3. ช่องขวา: เลือก **Comma-separated values (.csv)**
4. กด **Publish** → copy ลิงก์ที่ได้

### 4. ใส่ลิงก์ในเว็บ

เปิดไฟล์ `find-your-future.html` หาบรรทัด:

```js
const SHEET_CSV_URL = '';
```

วางลิงก์ที่ copy มา แล้ว push git ครั้งเดียว:

```bash
git add . && git commit -m "Connect FYF sheet" && git push
```

**เสร็จ!** หลังจากนี้แก้ที่ Sheet ได้ตลอด ไม่ต้อง push อีก

---

## 📋 คำอธิบายคอลัมน์

| คอลัมน์ | บังคับ | ตัวอย่าง | ใช้ทำอะไร |
|---------|-------|----------|-----------|
| `title` | ✅ | `ค่ายโอลิมปิกวิชาการ` | ชื่อกิจกรรม (แสดงบนการ์ด) |
| `category` | ❌ | `วิชาการ` | หมวด — สร้างปุ่มกรองอัตโนมัติ |
| `detail` | ❌ | `ค่ายอบรมเข้ม 5 วัน...` | รายละเอียด (แสดงใน modal) |
| `requirement` | ❌ | `ม.4-6 GPA 3.0+` | 🌸 **คุณสมบัติผู้สมัคร** |
| `fee` | ❌ | `ฟรี` หรือ `500 บาท` | ✨ **ค่าใช้จ่าย** |
| `link` | ❌ | `https://...` | ลิงก์สมัคร — แสดงเป็นปุ่ม |
| `apply_date` | ❌ | `8 – 22 มิ.ย. 2569` | 📝 **วันรับสมัคร** (คำนวณ pill "เหลือ X วัน") |
| `apply_date_iso` | ❌ | `2026-06-08~2026-06-22` | วันรับสมัครแบบ ค.ศ. (แม่นยำกว่า) |
| `event_date` | ❌ | `18 – 19 ก.ค. 2569` | 🎪 **วันจัดกิจกรรม** |
| `event_date_iso` | ❌ | `2026-07-18~2026-07-19` | วันจัดกิจกรรมแบบ ค.ศ. |

---

## ⌨️ วิธีกรอกแต่ละคอลัมน์

### 📅 วันที่ — เลือกวิธีที่ถนัด

**วิธี 1: แบบไทย (อ่านง่ายใน Sheet)**
```
apply_date:  8 – 22 มิ.ย. 2569
event_date:  18 – 19 ก.ค. 2569
```

**วิธี 2: แบบ ISO ค.ศ. (แม่นยำกว่า — แนะนำ)**
```
apply_date_iso:  2026-06-08~2026-06-22
event_date_iso:  2026-07-18~2026-07-19
```

> **ช่วงวัน:** ใช้ `~` (แบบ ISO) หรือ `–` `-` (แบบไทย) คั่นระหว่างวันต้น-ท้าย
> **วันเดียว:** ใส่แค่วันเดียวไม่ต้องมีเครื่องหมาย

### 🌸 คุณสมบัติ / ✨ ค่าใช้จ่าย — รองรับแบบหลายบรรทัด

**แบบ 1: เขียนบรรทัดเดียว**
```
requirement:  ม.4-6 GPA 3.0+
```

**แบบ 2: หลายข้อ (แสดงเป็น bullet list ในเว็บ)**

ใน cell ให้กด **Alt+Enter** เพื่อขึ้นบรรทัดใหม่:
```
requirement:  เป็นนักเรียนชั้น ม.4-6
              สนใจสาขาจิตวิทยา
              ได้รับความยินยอมจากผู้ปกครอง
```

หรือคั่นด้วย `;` `•` `·` `|`:
```
requirement:  ม.4-6; GPA 3.0+; มีแฟ้มผลงาน
```

### 🏷️ หมวด (category)

พิมพ์อะไรก็ได้ — ระบบจะสร้างปุ่มกรองให้อัตโนมัติ
- ถ้าอยากให้ปุ่มเรียงตามใจ ให้ใช้ชื่อเดิมๆ ในหลายแถว
- ตัวอย่างที่แนะนำ: `วิชาการ`, `กิจกรรม`, `อาสา`, `อื่นๆ`

### 🔗 ลิงก์ (link)

ใส่ URL เต็ม เช่น `https://forms.gle/XXXX` — เว็บจะแสดงเป็นปุ่ม "สมัคร / ดูรายละเอียด →"

---

## 🎨 ตัวอย่างข้อมูล

| title | category | apply_date_iso | event_date_iso | requirement | fee | link |
|-------|----------|----------------|----------------|-------------|-----|------|
| ค่ายปล่อยจิตครั้งที่ 12 | วิชาการ | 2026-06-08~2026-06-22 | 2026-07-18~2026-07-19 | ม.4-6; สนใจจิตวิทยา | ฟรี | https://forms.gle/... |
| ทุน กพ. 2569 | อื่นๆ | 2026-06-01~2026-06-30 |  | สัญชาติไทย; อายุ <18 | ฟรี | https://ocsc.go.th |
| Workshop เขียน Resume | กิจกรรม | 2026-05-25 |  | ม.6 เท่านั้น | ฟรี | https://... |

---

## 🎯 พฤติกรรม pill / status

pill "อีก X วัน" คำนวณจาก **`apply_date`** (วันรับสมัคร) — เพราะเป็นเดดไลน์ที่สำคัญกว่า

| สถานการณ์ | pill ที่แสดง |
|-----------|--------------|
| ยังไม่ถึงวันรับสมัคร | 🟡 "อีก 5 วัน" |
| อยู่ในช่วงรับสมัคร | 🟡 "กำลังเปิด · เหลือ 12 วัน" |
| วันสุดท้ายของการรับสมัคร | 🟡 "วันสุดท้าย" |
| วันนี้ / พรุ่งนี้ | 🟡 "วันนี้" / "พรุ่งนี้" |
| ผ่านวันรับสมัครแล้ว | ⚫ "ผ่านมา 3 วัน" |
| ไม่มีวันที่ใดๆ | 🌸 "ติดตามประกาศ" |

---

## 🔄 ระบบทำงานยังไง

```
1. คุณเปิด Google Sheet
   ↓
2. พิมพ์/แก้ข้อมูลในแถว
   ↓
3. Google Sheet auto-save (< 5 วิ)
   ↓
4. Google publish CSV cache (~1-5 นาที)
   ↓
5. คนเปิดเว็บ Find Your Future → JS fetch CSV → แสดง
```

**ไม่ต้อง push git!** แค่แก้ Sheet พอ

---

## ⚠️ ข้อควรระวัง

- **หัวคอลัมน์ต้องสะกดตรงเป๊ะ** (ตัวเล็ก, มี underscore) — ผิดตัวเดียวไม่รู้จัก
- **Sheet ที่ publish เป็นสาธารณะ** — ใครมีลิงก์ก็เข้าถึงได้ อย่าใส่ข้อมูลลับ
- **cache ~5 นาที** — แก้แล้วยังไม่เห็น รอสักครู่ + Hard refresh (Cmd+Shift+R)
- **ปล่อยว่างได้** — คอลัมน์ที่ไม่บังคับ ไม่ต้องกรอกทุกช่อง

---

## 🆘 ปัญหาที่เจอบ่อย

**Q: หน้าเว็บขึ้น "ยังไม่ได้ตั้ง Google Sheet"**
A: ยังไม่ได้ใส่ลิงก์ CSV ใน `SHEET_CSV_URL` ในไฟล์ `find-your-future.html`

**Q: แก้ Sheet แล้วเว็บไม่อัปเดต**
A: รอ 1-5 นาที + Hard refresh (Cmd+Shift+R หรือ Ctrl+Shift+R)

**Q: pill "อีก X วัน" ไม่ตรงกับความจริง**
A: เช็ค `apply_date_iso` ว่าเป็น `YYYY-MM-DD` และเป็นปี ค.ศ. (2026) ไม่ใช่ พ.ศ. (2569)

**Q: หมวด (category) ปุ่มกรองไม่ขึ้น**
A: ต้องมีอย่างน้อย 1 แถวที่ใส่ค่า category นั้นจริงๆ ระบบถึงจะสร้างปุ่มให้

**Q: รายละเอียดยาวเกินไป ไม่อยากให้ขึ้นในการ์ด**
A: การ์ดแสดงแค่ title + category + วันที่ + pill → รายละเอียดขึ้นแค่ใน **modal** (คลิกการ์ดเพื่อเปิด)

---

## 🎨 ฟีเจอร์เว็บ (สรุป)

- ✅ **การ์ด compact** — แสดง title + วันที่ + pill
- ✅ **คลิกการ์ด → เปิด modal** พร้อมข้อมูลเต็ม
- ✅ **ปุ่มกรองหมวด** อัตโนมัติ (จากคอลัมน์ category)
- ✅ **เรียงตามความใกล้เดดไลน์** (ใกล้สุดขึ้นบน, past อยู่ล่างสุด)
- ✅ **Deep link** — copy URL `.../find-your-future#ชื่อค่าย` ส่งให้เพื่อน กดเปิด modal ทันที
- ✅ **ลิงก์จากหน้าแรก** — การ์ด "ค่ายเร็วๆ นี้" คลิกแล้วไปเปิด modal ของค่ายนั้นทันที
